# HPC Final Project

Five implementations of the an edge-detecting image convolution operation on the mandelbrot set.

Build instructions:
`git clone https://github.com/bndkr/hpc-final.git`
`cd hpc-final`
`cmake -B _bld`
`cmake --build _bld --config release`
